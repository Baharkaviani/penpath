# Leave unset to use Vite proxy (/api/v1 → backend). Recommended for Docker dev.
# VITE_API_BASE_URL=/api/v1

# Only use direct backend URL if CORS is configured (see README):
# VITE_API_BASE_URL=http://localhost:8000/api/v1
